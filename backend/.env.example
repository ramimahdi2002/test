# Port the API listens on
PORT=4000

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=client_requests

# Database used by `npm test` (kept separate from DB_NAME)
TEST_DB_NAME=client_requests_test
